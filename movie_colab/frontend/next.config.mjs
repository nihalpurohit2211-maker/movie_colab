/** @type {import('next').NextConfig} */
const nextConfig = {
  // The frontend calls the backend directly from the browser
  // at NEXT_PUBLIC_API_URL (default http://localhost:8000).
};

export default nextConfig;